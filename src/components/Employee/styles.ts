import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    marginTop: 10,
    borderRadius: 4,
    backgroundColor: "#D9E0E2",
  },
  header: {
    fontSize: 20,
    fontWeight: "bold",
    paddingBottom: 10
  },
  text: {
    flexDirection: "row"
  }
});